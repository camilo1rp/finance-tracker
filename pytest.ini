[pytest]
pythonpath = .
testpaths = tests
addopts = --import-mode=importlib -m "not live_gmail and not live_gmail_rest"
markers =
    live_gmail: live Gmail MCP smoke test (deselected by default)
    live_gmail_rest: live Gmail REST smoke test (deselected by default)
